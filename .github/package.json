{
  "name": "specdir",
  "version": "1.0.0",
  "description": "The open directory of Spectral domain specifications",
  "scripts": {
    "validate": "node .github/scripts/validate-registry.js",
    "check-urls": "node .github/scripts/check-urls.js",
    "validate-spectral": "node .github/scripts/validate-spectral.js",
    "build": "node .github/scripts/generate-json.js && node .github/scripts/generate-site.js",
    "validate:all": "npm run validate && npm run check-urls && npm run validate-spectral"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
