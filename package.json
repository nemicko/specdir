{
  "name": "specdir",
  "version": "1.2.0",
  "description": "The open directory of ACL Features",
  "scripts": {
    "validate": "node .github/scripts/validate-registry.js",
    "check-urls": "node .github/scripts/check-urls.js",
    "validate-acl": "node .github/scripts/validate-acl.js",
    "build": "node .github/scripts/generate-json.js && node .github/scripts/generate-site.js",
    "validate:all": "npm run validate && npm run check-urls && npm run validate-acl"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
